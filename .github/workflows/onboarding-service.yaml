name: Tenant Onboarding Tool
on:
  push:
  pull_request:
 
jobs:
  Tenant-Generator:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: ./startup.sh -r
